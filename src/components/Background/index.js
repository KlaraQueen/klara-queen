import React from "react";
import { BackgroundWrapper, GlobalStyle } from "./styled";

function Background() {
  return (
    <BackgroundWrapper>
      <GlobalStyle />
    </BackgroundWrapper>
  );
}

export default Background;
